import React from 'react';
import './styles.scss';

const Loading = () => {
  return (
   <div className="spinner-wrapper">
     <span className="spinner"/>
    </div>
  );
};

export default Loading;
